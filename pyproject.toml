[project]
name = "spamdetector"
version = "0.1.0"
description = "Email spam detector: local ML filter + OpenAI confirmation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=5.0.1",
    "huggingface-hub>=1.27.0",
    "joblib>=1.5.3",
    "numpy>=2.5.1",
    "openai>=2.53.0",
    "pandas>=3.0.5",
    "python-dotenv>=1.2.2",
    "scikit-learn>=1.9.0",
]
